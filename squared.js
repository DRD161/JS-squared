// Define a function that takes a number as an argument 
var square = function(num) {

	// Returns that number times itself (square it)
	return num * num;
};
// Log the results to the JavaScript console
console.log(square(4)); 